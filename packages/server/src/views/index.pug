doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='theme-color', content='#000000')
    script(type='text/javascript', src=`http://localhost:3001/gallery-sample.js`)
    title Gallery Sample - Extend
  body
    noscript
      | You need to enable JavaScript to run this app.
    #gallery
    script(type='text/javascript').
      GallerySample.create(document.getElementById('gallery'), {
        extensionsSource: '#{extensionsSource}',
        extendHostUrl: '#{extendHostUrl}',
        extendContainer: '#{extendContainer}',
        extendToken: '#{extendToken}'
      });
